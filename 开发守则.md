# 智能待办系统 · 开发守则 V3.0

> 技术栈变更记录：V2.x 为单 HTML 文件方案；V3.0 起改为 **React SPA + Node.js 后端**  
> 日期：2026-05-26  
> 变更批准：用户书面确认（规格文档先行变更）

---

## 一、绝对禁止

### G-01 禁止在前端直接调用 πOS API 和 AI API

**React 代码不得直接 `fetch` πOS Task/Pipeline API 或 AI Chat API。所有外部 API 调用必须通过 Node.js 后端代理。**

```javascript
// ❌ 错误：前端直接调 AI
const res = await fetch('https://api.openai.com/v1/chat/completions', { ... });

// ✅ 正确：调后端代理接口
const res = await fetch('/api/ai/decompose', { method: 'POST', body: JSON.stringify({ input }) });
```

**为什么**：API Key 和 πOS Token 存在服务器端环境变量，不暴露到浏览器。

---

### G-02 禁止前端硬编码任何密钥或 Token

**React 前端不得包含 AI API Key、πOS Bearer Token 等凭据。所有密钥只存于 Node.js 后端的 `.env` 文件或环境变量中。**

---

### G-03 禁止绕过 Express 路由层直接暴露数据源

**所有状态读写必须经由 Node.js REST 接口层。前端不得直接访问任何持久化层。**

---

### G-04 禁止跳过 FrequencyAnalyzer 统计门控直接调 AI

**后端 `POST /api/ai/recommendations` 只能由 RecommendationService 在确认存在新候选模式后调用。不得暴露直接触发 AI 推荐的未门控接口。**

---

### G-05 禁止在管线创建失败时写入 keywordRules

**采纳推荐时原子性保证：后端 `createPipeline()` 成功拿到真实 `pipelineId` 后，才写 `keywordRules`。失败时两者都不写。**

---

### G-06 禁止在模糊时间表达上设置 dueAt

**`dueAt` 只在 AI 能计算出精确 Unix 时间戳时才填写。"有空时""改天" 等只填 `timeLabel`，`dueAt` 留空。**

---

### G-07 禁止 DecomposedTask 省略 text 和 correctedText

**后端 AI 拆解接口的响应中，`text` 和 `correctedText` 为必返回字段。fallback 模式下两者值相同，但不得省略。**

---

### G-08 禁止在前端 localStorage 存储敏感配置

**AI API Key、πOS Token 等敏感信息只能由用户配置后发至后端，由后端持久化到 `.env` 或加密存储。前端 localStorage 只存非敏感 UI 状态（filter 选项、主题偏好等）。**

---

## 二、技术栈约定

### T-01 前端：React + Vite

```
框架：React 19（hooks-only，不用 class 组件）
构建：Vite 6
状态：React Context + useReducer（规模不足以引入 Redux）
HTTP：原生 fetch（不引入 axios）
样式：CSS Modules（保持零外部 UI 库依赖）
```

**不允许引入**：jQuery、Lodash、moment.js、任何 UI 组件库（Ant Design / MUI / Chakra）

### T-02 后端：Node.js + Express

```
运行时：Node.js ≥ 20 LTS
框架：Express 5
持久化：JSON 文件（< 500 条任务规模，无需数据库）+ πOS Task API 主存
进程：pm2（生产）/ nodemon（开发）
```

**不允许引入**：TypeORM / Prisma（规模不需要）、Passport（自定义 Token 中间件即可）

### T-03 通信协议

```
前后端：REST + JSON
状态推送：SSE（Server-Sent Events）替代前端轮询定时器
```

### T-04 工程结构

```
/
├── frontend/          React Vite SPA
│   ├── src/
│   │   ├── components/   UI 组件（纯渲染）
│   │   ├── hooks/        业务逻辑 hooks
│   │   ├── services/     fetch 封装（api.js）
│   │   ├── context/      全局状态
│   │   └── main.jsx
│   └── vite.config.js
├── backend/           Node.js Express
│   ├── src/
│   │   ├── routes/       路由定义
│   │   ├── controllers/  请求/响应处理
│   │   ├── services/     业务逻辑
│   │   └── server.js
│   └── .env
└── shared/            共享类型（JSDoc）
```

---

## 三、架构层级规则

### E-01 前端三层：UI 组件 → Custom Hooks → API Service

```
components/     纯渲染，props 驱动，不直接 fetch
hooks/          业务逻辑，调用 services/api.js
services/api.js fetch 封装，统一 baseURL 和错误处理
```

### E-02 后端四层：Router → Controller → Service → External

```
routes/         路由注册
controllers/    req/res 处理，参数校验
services/       业务逻辑（AI、Pipeline、Task、Recommendation）
external/       πOS API、AI Chat API 的封装客户端
```

### E-03 新增 API 接口必须通过 Express Router 注册，不在 server.js 堆积路由

### E-04 AI 配置集中在后端 ConfigService；前端通过 `/api/config` 读取脱敏配置（不含密钥明文）

### E-05 状态推送：后端 SSE 替代前端 setInterval

```javascript
// 后端：SSE 推送管线状态变更
res.setHeader('Content-Type', 'text/event-stream');
res.write(`data: ${JSON.stringify({ taskId, pipelineStatus })}\n\n`);

// 前端：EventSource 接收，替代 PollingScheduler
const es = new EventSource('/api/pipeline/status-stream');
es.onmessage = e => dispatch({ type: 'UPDATE_PIPELINE_STATUS', payload: JSON.parse(e.data) });
```

### E-06 环境变量：所有密钥通过 `backend/.env` 管理，`.env` 加入 `.gitignore`

```
PIOS_API_BASE_URL=
PIOS_API_TOKEN=
AI_API_BASE_URL=https://api.openai.com
AI_API_KEY=
AI_MODEL=gpt-4o-mini
PORT=3000
```

### E-07 前端状态：React Context 单一数据源，禁止在多个组件内各自维护同一份数据

### E-08 响应式布局：CSS Media Queries，不在 JS 中检测 `window.innerWidth`

### E-09 错误处理统一：后端 `{ error: { code, message } }`，前端 Toast 展示

---

## 四、迁移自 V2.x 的功能映射

| V2.x 模块（单 HTML） | V3.0 落点 |
|--------------------|----------|
| StorageAdapter（localStorage） | 后端：JSON 文件持久化；前端 localStorage：仅 UI 状态 |
| TaskManager（πOS API 主存） | 后端 TaskService + πOS API |
| AIDecomposer | 后端 AIService.decompose() |
| FrequencyAnalyzer | 后端 RecommendationService |
| PipelineRouter | 后端 RouterService |
| PollingScheduler（setInterval） | 后端 SSE 流推送 |
| AppConfig（密钥 + 参数） | 后端 ConfigService（密钥）+ 前端 Context（UI 偏好） |
| 单 HTML 文件交付 | `frontend/dist/` 静态构建产物 + `backend/` Node.js 服务 |

---

## 五、不变的功能约束（继承自 V2.x）

以下约束的语义不变，实现层从浏览器内迁移到 Node.js 后端：

- **G-04**：推荐生成必须经过本地统计门控（移至后端）
- **G-05**：createPipeline 原子性（移至后端）
- **G-06**：dueAt 只填精确时间戳
- **G-07**：DecomposedTask text + correctedText 必填

以下约束保持在前端：

- **语音识别**：Web Speech API 仍在浏览器端（无法代理到后端）
- **G-08 变体**：XSS 防护——React JSX 默认转义，后端响应 CSP 头
- **E-08**：响应式布局纯 CSS 实现
